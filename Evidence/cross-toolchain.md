# Cross-toolchain check

No additional official Swift.org toolchain is installed on this host. The available compiler is Apple Swift 6.3.3 from Xcode 26.6, and `/Library/Developer/Toolchains` plus the user toolchain directory contain no alternate toolchain. Installing a second toolchain would change the environment and was not done. The cross-toolchain comparison is therefore explicitly unresolved, not claimed as passed.
